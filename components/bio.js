import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioSection = styled(Box)`
  padding-left: 10.18em;
  text-indent: -10.18em;
`

export const BioYear = styled.span`
  font-weight: bold;
  margin-right: 1em;
`